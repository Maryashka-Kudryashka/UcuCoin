import React from "react";
import { BrowserRouter } from "react-router-dom";

import Layout from "./Layout";

const Root = () => (
  <BrowserRouter>
    <Layout />
  </BrowserRouter>
);

export default Root;
